这是前五项里面我花了最多时间的项目，运用了之前搭建的对话网站的部分代码，总之现在可以通过提示词自己生成图片了。
可能是模型自身原因，生成的图片偏向中式水墨画风。
网站里可以直接看到图片，下载到本地的图片统一放在static\generated文件夹里边了。
从Hugging Face上获取的TOKEN有时会失效，需要在.env里手动更新（实在没法自动获取）。
模型不建议切换因为别的根本用不了嘻嘻。